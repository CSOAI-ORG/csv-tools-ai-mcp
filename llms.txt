# Csv Tools Ai MCP

> Csv Tools MCP — automation tooling for csv tools

`csv-tools-ai-mcp` is a Model Context Protocol (MCP) server by MEOK AI Labs.

## Install

```bash
pip install csv-tools-ai-mcp
```

Or run without installing:

```bash
uvx csv-tools-ai-mcp
```

## Tools

- `parse_csv` — Parse CSV content and return structured data with statistics.
- `validate_headers` — Validate that CSV headers match expected column names.
- `detect_delimiter` — Auto-detect the delimiter used in a CSV/DSV file.
- `convert_to_json` — Convert CSV content to JSON array of objects.

## Links

- Source: https://github.com/CSOAI-ORG/csv-tools-ai-mcp
- PyPI: https://pypi.org/project/csv-tools-ai-mcp/
- MEOK AI Labs: https://meok.ai
- Council of AI: https://councilof.ai
